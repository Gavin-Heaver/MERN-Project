name: Deploy API

on:
    push:
        branches: [main]
        paths:
            - 'api/**'
    workflow_dispatch:

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Trigger Render deploy
              run: curl ${{ secrets.RENDER_DEPLOY_HOOK }}